## Summary

Describe the change and why it is needed.

## Test Commands

List the commands you ran, for example:

```bash
swift build
.build/debug/quake-probe --self-test
.build/debug/quake-probe --all-hid
```

## Hardware Notes

If this touches HID, display, input, output, or panel placement, include:

- Mac model:
- macOS version:
- DK display resolution / rotation:
- `quake-probe --all-hid` output, if relevant:

## Safety Check

- [ ] This does not add or expose DFU / firmware-flash commands.
- [ ] Any new device output command is documented and reversible.
- [ ] Any new plugin/host capability is mediated by permissions.
